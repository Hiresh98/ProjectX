apiVersion: v1
kind: Service
metadata:
  name: {{ include "projectx.fullname" . }}
  labels:
    {{- include "projectx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "projectx.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
