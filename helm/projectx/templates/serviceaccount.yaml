{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "projectx.serviceAccountName" . }}
  labels:
    {{- include "projectx.labels" . | nindent 4 }}
{{- end }}
